export const subscriptionStatusValues = ["active", "paused"] as const;
export const clientSubscriptionStatusValues = ["active", "paused"] as const;
export const userPlanValues = ["FREE", "PREMIUM"] as const;
