{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "22",
    "bun": "1.3.10"
  },
  "steps": {
    "build": {
      "commands": [
        "bun install",
        "bun run build",
        "cp -r public .next/standalone/public",
        "cp -r .next/static .next/standalone/.next/static",
        "cp -r node_modules/@github/copilot .next/standalone/copilot-cli"
      ],
      "variables": {
        "NEXT_CPU_COUNT": "4"
      }
    }
  },
  "deploy": {
    "startCommand": "bun .next/standalone/server.js",
    "variables": {
      "NODE_ENV": "production",
      "HOSTNAME": "0.0.0.0",
      "AUTH_TRUST_HOST": "true"
    }
  }
}
